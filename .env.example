# Database Configuration
DB_PASSWORD=your-secure-database-password-here

# JWT Secrets (CHANGE THESE IN PRODUCTION!)
JWT_SECRET=your-jwt-secret-at-least-32-characters-long
JWT_REFRESH_SECRET=your-different-refresh-secret-at-least-32-characters

# JWT Expiration
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# API Configuration
PORT=3000
NODE_ENV=development
BASE_URL=http://localhost:3000

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Frontend Configuration
VITE_API_URL=http://localhost:3001

# Initial Admin User (for seeding database)
INITIAL_ADMIN_EMAIL=admin@phm.local
INITIAL_ADMIN_PASSWORD=ChangeThisImmediately123!

# Optional: AI Features (for Phase 2)
# GEMINI_API_KEY=your-gemini-api-key-here
# DEEPGRAM_API_KEY=your-deepgram-api-key-here

# Optional: Email Configuration (for Phase 3)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-email@example.com
# SMTP_PASSWORD=your-smtp-password
# SMTP_FROM=noreply@phm.local
